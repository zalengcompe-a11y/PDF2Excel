name: Build & Release

# Trigger: push a version tag  →  git tag v2.0.0 && git push origin v2.0.0
# Or: run manually from GitHub Actions tab
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. v2.0.0)'
        required: true
        default: 'v2.0.0'

# Allow workflow to create/update GitHub Releases and upload files
permissions:
  contents: write

jobs:
  # ── Windows: build PDF2Office.exe ─────────────────────────────────────────
  build-windows:
    name: Build Windows (.exe)
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build .exe with PyInstaller
        run: >
          python -m PyInstaller PDF2Excel.spec --noconfirm
          --workpath "$env:TEMP\PDF2Office_build\work"
          --distpath dist

      - name: Create zip
        run: Compress-Archive -Path dist\PDF2Office -DestinationPath PDF2Office_windows.zip -Force

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: PDF2Office_windows.zip
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ── macOS: build PDF2Office.app ────────────────────────────────────────────
  build-macos:
    name: Build macOS (.app)
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build .app with PyInstaller
        run: python -m PyInstaller PDF2Excel_mac.spec --clean --noconfirm

      - name: Create zip
        run: |
          cd dist
          zip -r -9 ../PDF2Office_mac.zip PDF2Office.app -x "*.DS_Store" -x "*/__pycache__/*" -x "*/test*"

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: PDF2Office_mac.zip
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
